#!/usr/bin/env python
from setuptools import setup


if __name__ == '__main__':
    setup(
        name='tsexp',
        version='0.0.0',
        description='TS models',
        author='Pierre Delaunay',
        packages=[
            'tsexp',
        ],
        setup_requires=['setuptools'],
    )
